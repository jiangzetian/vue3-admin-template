import { createStore } from 'vuex';
import loading from '@/store/modules/loading';

export default createStore({
    state: {},
    mutations: {},
    actions: {},

    modules: {
        loading,
    },
});
