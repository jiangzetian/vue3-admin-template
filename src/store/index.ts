import { createStore } from 'vuex';
import loadings from '@/store/moduls/loading';

export default createStore({
    state: {},
    mutations: {},
    actions: {},
    modules: {
        loadings,
    },
});
