import { createStore } from 'vuex';
import loading from '@/store/moduls/loading';

export default createStore({
    state: {},
    mutations: {},
    actions: {},

    modules: {
        loading,
    },
});
